[motors_sync]
axes: x,y
accel_chip: adxl345


[gcode_macro SYNC_MOTORS]
rename_existing: _SYNC_MOTORS
gcode:
  {% if not printer.motors_sync.applied %}
    _SYNC_MOTORS
  {% else %}
    {action_respond_info("Motors already synchronized")}
  {% endif %}
